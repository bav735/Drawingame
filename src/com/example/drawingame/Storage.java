/*
package com.example.drawingame;


import co.realtime.storage.StorageRef;
import co.realtime.storage.ext.StorageException;
import ibt.ortc.extensibility.OnReconnected;
import ibt.ortc.extensibility.OrtcClient;

public class Storage {
    private StorageRef storageRef;
    private MainActivity mainActivity;

    private OnReconnected onReconnected = new OnReconnected() {

        @Override
        public void run(OrtcClient ortcClient) {

        }
    };

    public Storage(MainActivity mainActivity) throws StorageException {
        this.mainActivity = mainActivity;
        storageRef = new StorageRef(MainActivity.applicationKey, null, "PM.Anonymous", true, false, "https://storage-balancer.realtime.co/server/ssl/1.0");
    }
}
*/
